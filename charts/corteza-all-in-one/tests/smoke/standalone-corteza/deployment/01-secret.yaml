apiVersion: kuttl.dev/v1beta1
kind: TestStep
apply:
- manifests/extra-secret.yaml
- manifests/extra-cm.yaml
assert:
- 01-assert.yaml
unitTest: false
commands:
- command: >
    helm upgrade corteza -n corteza-test "$PWD"/charts/corteza-all-in-one
    --set corteza.server.extraEnvFrom[0].configMapRef.name=extra-cm
    --set corteza.server.extraEnvFrom[1].secretRef.name=extra-secret
    --reuse-values